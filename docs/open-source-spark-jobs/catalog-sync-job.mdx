---
title: Data Catalog Sync Job
description: The Data Catalog Sync Job indexes tables across all catalogs, making them searchable and manageable in the Data Catalog.
last_update:
  date: 04/28/2026
  author: Shashank Chaudhary
---

import FlexButton from "@site/src/components/FlexButton";
import Img from "@site/src/components/Img";
import { Plus } from "@phosphor-icons/react";

---

The **Data Catalog Sync Job** scans all catalogs in your IOMETE environment and indexes their tables. Once indexed, tables appear in the **Data Catalog** page where you can add documentation, assign owners, define classifications, and configure maintenance properties.

**Latest version:** `5.0.0` (requires IOMETE `3.16.x` or later) &nbsp;·&nbsp; [View on GitHub](https://github.com/iomete/iomete-marketplace-jobs/tree/main/iomete-catalog-sync)

## Installation

### From the Marketplace

Navigate to **Job Templates** and click **Marketplace**. Find the **catalog-sync** card, click the **⋮** menu, and select **Deploy**.

<Img
  src="/img/spark-job/marketplace/catalog-sync/marketplace-deploy.png"
  alt="Deploy catalog-sync from the Marketplace"
/>

### Manual Setup

Go to **Job Templates** in the sidebar and click <FlexButton label='New Job Template' primary><Plus size={16} /></FlexButton>.

<Img
  src="/img/spark-job/marketplace/catalog-sync/create-general.png"
  alt="Job Templates page with New Job Template button"
/>

#### General

<Img
  src="/img/spark-job/marketplace/catalog-sync/form-general.png"
  alt="General settings — name, schedule, concurrency"
/>

- **Name:** `catalog-sync`
- **Schedule:** `0 * * * *` — runs every hour. Adjust to any cron expression that fits your needs.
- **Concurrency:** `Forbid` — prevents overlapping runs if a previous execution is still running.

#### Application

<Img
  src="/img/spark-job/marketplace/catalog-sync/form-application.png"
  alt="Application settings — docker image, main class"
/>

- **Application type:** `JVM (Java, Scala)`
- **Docker image:** `iomete.azurecr.io/iomete/iom-catalog-sync:5.0.0`
- **Main Class:** `com.iomete.catalogsync.App`
- **Main application file:** `spark-internal`

#### Configurations

Add a Config Map entry so the job can read your exclusion rules at runtime:

<Img
  src="/img/spark-job/marketplace/catalog-sync/form-config.png"
  alt="Config Maps tab — file path and exclusion rules JSON"
/>

- **File path:** `/etc/configs/application.json`
- **Content:** your exclusion rules JSON (see [Exclusion Rules](#exclusion-rules))

#### Instance

<Img
  src="/img/spark-job/marketplace/catalog-sync/form-instance.png"
  alt="Instance settings — deployment type, node driver, executor"
/>

- **Deployment type:** `Standard`
- **Node driver / Node executor:** any small instance type is sufficient (e.g. `t3.micro 2vCPU/1GiB`)
- **Executor count:** `1`

Click <FlexButton label="Create" primary /> to save the job.

## How It Works

The job runs on a schedule (every hour by default) and scans all catalogs in your environment, indexing every table it finds. You can adjust the schedule or trigger a run manually at any time. Assets excluded by an [exclusion rule](#exclusion-rules) are skipped during each run.

## Running the Job

The job runs automatically on its schedule. To trigger it manually, open the job and click <FlexButton label='Run job' primary/>.

<Img
  src="/img/spark-job/marketplace/catalog-sync/job-run.png"
  alt="Run Catalog Sync Job manually"
/>

## Deleted Tables

When a table is dropped from a catalog, it is not removed from the Data Catalog index. Instead, it is marked as **deleted** and hidden from the default view. Use the **Deleted** toggle in the Data Catalog UI to review what was removed and when.

<Img
  src="/img/spark-job/marketplace/catalog-sync/data-catalog-deleted.png"
  alt="Data Catalog deleted tables view"
/>

:::note
Deleted entries are currently kept indefinitely. Automatic cleanup of old deleted entries is planned for a future release.
:::

## Configuration

The job reads its exclusion rules from `/etc/configs/application.json`. Add this via **Configurations → Config Maps** in the job form — this applies whether you installed from the Marketplace or created the job manually.

If no config file is provided, the job indexes everything using the built-in default rule (see below).

## Exclusion Rules

Not every catalog needs to be indexed. For example, you may want to skip:

- An external catalog (e.g., Oracle) with millions of tables that would clutter search results
- Catalogs or schemas containing sensitive data that shouldn't appear in the Data Catalog
- Personal or sandbox environments used for development

You can configure exclusion rules in the job configuration to skip specific catalogs, namespaces (schemas/databases), or tables during sync.

### Default Rule

The default rule applies at **all levels** — catalogs, schemas, and tables. If an asset has a matching property, it is excluded from indexing.

```json
{
  "exclusion_rules": {
    "default_rule": {
      "filter_by_properties": {
        "iomete.governance.index": "false"
      }
    }
  }
}
```

With this rule in place, setting the property `iomete.governance.index=false` on any catalog, schema, or table will exclude it from the sync.

This is the simplest setup — define one rule and apply it wherever needed using properties.

### Excluding Catalogs

Catalogs can be excluded **by name**, **by property**, or both.

#### By name

List catalog names directly. These catalogs will always be skipped, regardless of their properties:

```json
{
  "exclusion_rules": {
    "catalogs": {
      "names": ["sandbox_dev", "legacy_warehouse", "user_personal_spaces"]
    }
  }
}
```

#### By property

Set the `iomete.governance.index` property to `false` on the catalog in the **Admin Portal** when creating or editing it.

{/* TODO: Add screenshot of setting catalog property in Admin Portal */}

Then add the corresponding filter:

```json
{
  "exclusion_rules": {
    "catalogs": {
      "filter_by_properties": {
        "iomete.governance.index": "false"
      }
    }
  }
}
```

#### Combining both

You can use names and properties together. A catalog is excluded if it matches **either** condition:

```json
{
  "exclusion_rules": {
    "catalogs": {
      "names": ["sandbox_dev", "legacy_warehouse"],
      "filter_by_properties": {
        "iomete.governance.index": "false"
      }
    }
  }
}
```

### Excluding Namespaces (Schemas/Databases)

Namespaces are excluded by property. To mark a namespace for exclusion, set the property using SQL:

```sql
ALTER DATABASE my_catalog.my_schema SET DBPROPERTIES ('iomete.governance.index' = 'false');
```

To verify the property:

```sql
DESCRIBE DATABASE EXTENDED my_catalog.my_schema;
```

To remove the property later (re-enable indexing):

```sql
ALTER DATABASE my_catalog.my_schema SET DBPROPERTIES ('iomete.governance.index' = 'true');
```

Then add the exclusion rule:

```json
{
  "exclusion_rules": {
    "schemas": {
      "filter_by_properties": {
        "iomete.governance.index": "false"
      }
    }
  }
}
```

### Excluding Tables

Tables work the same way as namespaces — set a property on the table, then define the matching rule.

```sql
ALTER TABLE my_catalog.my_schema.my_table SET TBLPROPERTIES ('iomete.governance.index' = 'false');
```

To verify the property:

```sql
SHOW TBLPROPERTIES my_catalog.my_schema.my_table;
```

To remove the property later:

```sql
ALTER TABLE my_catalog.my_schema.my_table UNSET TBLPROPERTIES ('iomete.governance.index');
```

Then add the exclusion rule:

```json
{
  "exclusion_rules": {
    "tables": {
      "filter_by_properties": {
        "iomete.governance.index": "false"
      }
    }
  }
}
```

#### Multiple properties (OR logic)

When multiple properties are defined in a table exclusion rule, a table is excluded if **any one** of the properties matches:

```json
{
  "exclusion_rules": {
    "tables": {
      "filter_by_properties": {
        "iomete.governance.index": "false",
        "hidden": "true"
      }
    }
  }
}
```

Here, a table is excluded if `iomete.governance.index` is `false` **or** `hidden` is `true`. It does not need to match both.

### Full Example

A complete configuration using all exclusion levels:

```json
{
  "exclusion_rules": {
    "catalogs": {
      "names": ["sandbox_dev", "legacy_warehouse", "user_personal_spaces"],
      "filter_by_properties": {
        "iomete.governance.index": "false"
      }
    },
    "schemas": {
      "filter_by_properties": {
        "iomete.governance.index": "false"
      }
    },
    "tables": {
      "filter_by_properties": {
        "iomete.governance.index": "false",
        "hidden": "true"
      }
    },
    "default_rule": {
      "filter_by_properties": {
        "iomete.governance.index": "false"
      }
    }
  }
}
```
